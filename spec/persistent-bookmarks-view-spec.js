'use babel';

import PersistentBookmarksView from '../lib/persistent-bookmarks-view';

describe('PersistentBookmarksView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
